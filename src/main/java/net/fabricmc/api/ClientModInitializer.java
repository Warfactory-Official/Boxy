package net.fabricmc.api;

// Stub: Voxy's VoxyClient implements this. Boxy never invokes it (Voxy self-bootstraps via mixins).
public interface ClientModInitializer {
    void onInitializeClient();
}
